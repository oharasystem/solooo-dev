---
description: 変更を確認し、git add, commit, push までを一括で実行する
---

// turbo-all

1. git status を実行して、変更されたファイルを確認します。
2. git diff を実行して、変更内容の詳細を確認します。
3. 変更内容に基づいた簡潔で適切なコミットメッセージを生成し、 `git add . && git commit -m "メッセージ" && git push` を実行します。
